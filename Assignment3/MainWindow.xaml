<Window x:Class="Assignment3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Assignment3"
        mc:Ignorable="d"
        Title="Sprite Sheet Generator" Height="500" Width="800">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:PathToImageConverter x:Key="PathToImageConverter"/>

        <!-- Style for TextBoxes -->
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <!-- Input Binding for Hotkeys -->
    <Window.InputBindings>
        <KeyBinding Key="Z" Modifiers="Ctrl" Command="{Binding UndoCommand}"/>
        <KeyBinding Key="Y" Modifiers="Ctrl" Command="{Binding RedoCommand}"/>
        <KeyBinding Key="Delete" Command="{Binding RemoveCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <!-- Menu -->
        <Menu DockPanel.Dock="Top" Background="#F0F0F0">
            <MenuItem Header="File">
                <MenuItem Header="New" Command="{Binding NewCommand}"/>
                <MenuItem Header="Open..." Command="{Binding OpenCommand}"/>
                <MenuItem Header="Save" Command="{Binding SaveCommand}"/>
                <MenuItem Header="Save As..." Command="{Binding SaveAsCommand}"/>
                <Separator/>
                <MenuItem Header="Exit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Undo" Command="{Binding UndoCommand}" InputGestureText="Ctrl+Z"/>
                <MenuItem Header="Redo" Command="{Binding RedoCommand}" InputGestureText="Ctrl+Y"/>
                <Separator/>
                <MenuItem Header="Remove" Command="{Binding RemoveCommand}"/>
                <MenuItem Header="Remove All" Command="{Binding RemoveAllCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Main Content -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Project Title -->
                <RowDefinition Height="Auto"/>
                <!-- Inputs -->
                <RowDefinition Height="*"/>
                <!-- Image List -->
                <RowDefinition Height="Auto"/>
                <!-- Progress Bar -->
            </Grid.RowDefinitions>

            <!-- Project Title -->
            <TextBlock Text="{Binding ProjectNameTitle}" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Inputs Grid -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Output Directory -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Output Directory"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" Name="tbOutputDir"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Width="80" Command="{Binding BrowseCommand}"/>

                <!-- Filename -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Filename (.png)"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding OutputFile, UpdateSourceTrigger=PropertyChanged}" Name="tbOutputFile"/>

                <!-- Columns & Checkbox -->
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Columns" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBox Width="100" Text="{Binding Columns}" Name="tbColumns"/>
                    <CheckBox Content="include meta data (.json)" 
                              IsChecked="{Binding IncludeMetaData}" 
                              VerticalAlignment="Center" Margin="20,0,0,10"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Horizontal Scrolling ListBox -->
                <ListBox Grid.Column="0" ItemsSource="{Binding Images}" SelectedItem="{Binding SelectedImage}" 
                         ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="2" Background="White">
                                <StackPanel>
                                    <Image Source="{Binding Converter={StaticResource PathToImageConverter}}" 
                                           Height="100" Width="100" Stretch="Uniform"/>
                                    <!-- Tooltip to show full path -->
                                    <StackPanel.ToolTip>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel.ToolTip>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0" Width="100">
                    <Button Content="Add" Command="{Binding AddCommand}"/>
                    <Button Content="Remove" Command="{Binding RemoveCommand}"/>
                    <Button Content="Generate" Command="{Binding GenerateCommand}" Margin="5,20,5,5"/>
                </StackPanel>
            </Grid>

            <!-- Bonus: Progress Bar -->
            <ProgressBar Grid.Row="3" Height="10" IsIndeterminate="True" 
                         Visibility="{Binding IsGenerating, Converter={StaticResource BooleanToVisibilityConverter}}"
                         Margin="0,10,0,0"/>
        </Grid>
    </DockPanel>
</Window>